#pragma once
#include "interface.h"

namespace Interface
{
	class ParticleSettings : public InterfaceBase
	{
		__DeclareClass(ParticleSettings)
	public:
		ParticleSettings();
		~ParticleSettings();

		void Run();

	private:

	};
}